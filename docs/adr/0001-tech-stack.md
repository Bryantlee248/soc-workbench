# ADR-0001：项目形态与技术栈

- 状态：proposed / accepted
- 日期：YYYY-MM-DD
- 决定人：<owner>

## Context（为什么现在要定）
- 项目形态：Web / 脚本工具 / 数据处理 / 桌面 / AI 应用？（单选或多选）
- 立项结论（K17 三问）：谁用 / 解决什么问题 / 怎么算成功。
- 当前约束：团队能力、已有资产、部署目标（229/内网/本地）、时间成本。

## Decision（决定）
- 形态：<例如 Web 系统>
- 技术栈：<例如 后端 Java/Spring + 前端 Vue + PostgreSQL>
- 一句话（Y-statement）：
  在 `<场景>` 中，面对 `<顾虑>`，我们决定用 `<X>` 而非 `<Y/Z>`，以达成 `<质量目标>`，接受 `<代价>`。

## Consequences（后果，好坏都写）
### 变容易的
- ...

### 变困难/代价
- ...

## Considered Options（考虑过的备选 + 为何否决）
- `<Y>`：否决，因为 ...
- `<Z>`：否决，因为 ...
