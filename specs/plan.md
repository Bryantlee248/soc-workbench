# Plan（技术方案 · 轻量）

> 只定「跨旅程共享的骨架」，不写每条旅程的全部细节（那在实现时做）。上限 ~1 页。

## 技术栈
（一行引用 docs/adr/0001，不重复）

## 架构分层
（后端分层 + 前端结构，几句话）

## 核心数据模型
（实体 + 关系，只列跨旅程共享的）

## 接口约定
（命名规范 + 关键接口；contracts/ 按需）

## 前端组件 / 信息架构
（页面结构 + 组件库选型 + 复用组件）★ 01 教训：上次漏了这块

## 共享约定
（权限模型 / 审计留痕 / 错误处理）
