{
  "name": "project-scaffold",
  "private": true,
  "type": "module",
  "scripts": {
    "converge": "node tools/converge.mjs",
    "verify": "node tools/verify.mjs",
    "test": "echo \"TODO: real tests\" && exit 0",
    "build": "echo \"TODO: real build\" && exit 0",
    "lint": "echo \"TODO: real lint\" && exit 0"
  }
}
